export const UI_COLORS = {
    primary: 'yellow-400',
    cardBg: 'bg-gray-900/60',
    cardBorder: 'border-gray-800',
};

export const MOTION = {
    fadeUp: { initial: { y: 8, opacity: 0 }, animate: { y: 0, opacity: 1 }, transition: { duration: 0.36 } },
};

export const NETWORK = {
    minLatencyMs: 200,
    maxLatencyMs: 600,
};

